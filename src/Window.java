import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

class Window extends JFrame {
    public Window() {
        setTitle("XMail");
        setSize(800, 600);
        setDefaultCloseOperation(EXIT_ON_CLOSE);

        setVisible(true);
    }
}